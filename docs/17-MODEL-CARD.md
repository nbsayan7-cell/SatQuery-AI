# 🤖 SatQuery AI — Model Cards

> **Purpose:** Every AI model we use gets a card. Makes the project credible and prevents
> fabricated accuracy (RULE 005). Accuracy fields stay "TBD" until measured by a named
> script on a named dataset.

---

## Model card template
Model: <name / HF id> Task: VQA / captioning / grounding / change / fusion Input: <format, bands, resolution> Output: <text / box / mask / map> Base / training:<pretraining + any adaptation we did> Dataset: <what it was trained/adapted on> License: Accuracy: <metric = value, measured by scripts/... on — or TBD> Limitations: Hardware: <min GPU/CPU, VRAM> Inference time: