## SQ-003 — Image Preprocessing Pipeline
Phase: 4 · Priority: P0 · Status: 🔴 · Requirement: (enables R1–R4) Goal: Centralize all image preparation so every model receives a consistent input. Files: ai/preprocessing.py Dependencies: SQ-001 Acceptance criteria: ✓ Converts multi-band GeoTIFF to a 3-channel RGB tensor via defined band selection ✓ Handles PNG/JPEG (already RGB) without breaking ✓ Resizes and normalizes to the model's expected input ✓ Exposes a single infer-ready function used by all models ✓ Raises a clear, catchable error on unsupported input Test: tests/test_preprocessing.py — feed 3-band and 13-band samples, assert output shape. Docs to update: CODEBASE.md §5, TROUBLESHOOTING.md (if it resolves a format bug), CHANGELOG.md Note: This ticket exists because band-mismatch is the classic 500 error (see TROUBLESHOOTING BUG #004).