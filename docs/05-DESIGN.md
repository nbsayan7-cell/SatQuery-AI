# 🎨 SatQuery AI — Design System (Visual Source of Truth)

> **Purpose:** So the UI stays consistent no matter which teammate or agent builds a
> panel. If it's not defined here, don't invent it — add it here first, then build.

**Version:** 1.0 · **Last updated:** <FILL:date>

---

## Design Principle

**Professional scientific instrument, not a consumer app.** It should feel like mission
control: dark, precise, data-forward, calm. Flashy 3D belongs in God's Eye, not the core.

## Color Tokens
| Token | Value | Use |
|-------|-------|-----|
| `--bg-space` | `#0A0E1A` | app background |
| `--panel` | `#131829` | panels/cards |
| `--accent` | `#3DD6D0` | primary actions, highlights |
| `--accent-warn` | `#F2B84B` | medium confidence |
| `--danger` | `#E5484D` | errors, low confidence |
| `--text-primary` | `#E6EBF5` | main text |
| `--text-muted` | `#8A93A8` | labels |
| <FILL more> | | |

## Typography
- UI: <FILL: e.g. Inter>. Mono (for data/trace): <FILL: e.g. JetBrains Mono>.
- Scale: 12 / 14 / 16 / 20 / 28 / 40.

## Layout